DEBUG: False

paths:
  metadata_csv: '/home/vito/fortunom/projects/AI4Health/data/fortunom/covidxct/proc_slices_metadata.csv'
  image_column: "image_path"

labeling:
  attribute_columns: ["sex", "age_group"]  # ["sex"] or ["age_group"], or ["sex", "age_group"]
  normalize_labels: True  # whether to encode string labels to integers

training:
  num_runs: 5
  num_epochs: 20
  batch_size: 128
  learning_rate: 0.001
  val_interval: 1

split:
  train_size: 0.9
  val_size: 0.1
  test_size: 0
  stratify: True

output:
  results_dir: "results/covid_experiments"
  save_models: True # needs to be True